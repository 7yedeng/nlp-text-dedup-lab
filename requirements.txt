# 实验运行环境
#
# 安装：pip install -r requirements.txt
#
# 本环境为实际验证通过的版本组合（Python 3.13.5 / Windows），四个实验已完整跑通。
# 原项目在 Python 3.8 下开发（numpy 1.23.2 / scikit-learn 1.3.2）；
# 注意 scikit-learn 1.3.2 → 1.8.0 之间 TfidfVectorizer 的内部实现有调整，
# 同一语料下的平均 TF-IDF 权重会有约 1%~4% 的差异（词表内容与词频完全一致），
# 因此报告中的数值以本环境重跑结果为准。

# 数值计算与机器学习
numpy>=2.0
scipy>=1.13
scikit-learn>=1.4

# 中文分词
jieba==0.42.1

# 可视化
matplotlib>=3.8

# 预训练词向量加载（实验二(1)）
gensim>=4.3

# 语料抓取（数据获取环节，可选）
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# 报告与代码截图生成（可选）
python-docx>=1.1
Pillow>=10.0
